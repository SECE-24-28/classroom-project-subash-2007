// Converted from types.ts — runtime JS equivalents (no TypeScript types)

// Exporting placeholder objects/constants for runtime use if needed.
export const PROVIDER_NAMES = ['JIO', 'AIRTEL', 'VI', 'BSNL'];

// The rest of the TypeScript-only declarations were removed.
// Keep this file so imports to './types' continue to work if any remain.
